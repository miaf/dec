<h1>Forgot Password</h1>
<%= form_tag(forgot_password_path, method: :put) do %>
	<p>
		<%= label_tag :email_or_login_name %>
		<%= text_field_tag :email_or_login_name, params[:email_or_login_name] %>
	    <%= submit_tag I18n.t('buttons.send') %>
	</p>
<% end %>
